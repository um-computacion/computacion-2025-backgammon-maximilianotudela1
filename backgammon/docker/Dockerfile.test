FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r requirements.txt && pip install pytest pytest-cov
COPY . /app
CMD ["pytest", "--cov=core", "--maxfail=1", "-q"]
